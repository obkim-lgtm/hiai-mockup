# 2026-09-02 — 채점기준 AI 검토 (v8에 얹음)

작업본 `../output/evaluation_design_v8.html` · **배포됨**(GitHub Pages, 커밋 `885cb80`)

## ▶ 이어받기 — 내일 여기서부터

**지금 상태**: 채점기준 AI 검토를 v8에 붙였고, **결과 화면 시안 2종이 나란히 살아 있다.**
둘 중 하나를 고르는 게 다음 결정이다. 둘 다 배포돼 있으니 링크로 비교하면 된다.

| | 시안1 `#case=rv-hit` | 시안2 `#case=rv-page` |
|---|---|---|
| 모습 | 우측 드로워 800px | 별도 화면(`screen-review`) 좌우 비교 |
| 오른쪽에 오는 것 | AI 코멘트 + 제안 문장 1줄 | **AI가 다듬은 채점기준 통째로** |
| 교사가 하는 일 | 채점기준 전체를 펴고 필드를 고침 | 골라서(체크) 반영, 오른쪽도 고칠 수 있음 |
| 확정 버튼 | `채점기준에 반영` | `수정된 채점기준 적용 (n)` |
| **지금 API로 되나** | **○** | **✕ — 맥에게 요청해야 함** |

배포본 — [시안1](https://obkim-lgtm.github.io/hiai-mockup/output/evaluation_design_v8.html#case=rv-hit) ·
[시안2](https://obkim-lgtm.github.io/hiai-mockup/output/evaluation_design_v8.html#case=rv-page)

### 다음에 할 일 (우선순위)

1. **stale·잠금은 확정·구현 끝.** 목업 보고 "안내가 너무 적다" 싶으면
   `SHOW_STANDING_NOTICE = true` 한 줄로 되살아난다(아래 "stale·잠금" 절)
2. **맥에게 확인** — 시안2가 성립하려면 API가 `제목 + 배점 배분 + 세부기준 전부`를 줘야 한다.
   지금은 `split_into`(축 이름)뿐. **"만점을 지키며 배점을 나눌 것"** 을 제약으로 걸어 달라고 할 것.
   시안1만 쓰더라도 `revised_descriptions`가 실제 응답에 있는지는 확인해야 한다(`적용` 버튼이 여기 달림)
3. **시안 택1** → 안 고른 쪽 케이스·코드 제거
4. **PRD H-2608 갱신** — V1.0에 이 기능이 통째로 없다. 세 단계 "왜 하는지"도 옛 문구(1-1·1-2 경계)라 같이 고쳐야
5. `output/index.html` 목업 목록 카드 — 올립이 "픽스했다" 할 때만 v8로 갱신(아직 v7 카드)

## stale·잠금 — **확정하고 구현함** (26-09-02 퇴근 전 결정)

올립 요구: **클릭 최소 + 개발 최소.** 복잡하게 넣으면 기간 내 배포를 못 한다.
두 목표가 같은 답을 가리켰다 — 막는 코드를 걷어내면 클릭도 개발도 준다.

### 원칙 3줄

1. **막지 않는다.** 인식 후 활동지 교체 잠금을 풀었다. 막으면 대안이 `복사해서 만들기`(=처음부터 다시)라 클릭 폭증
2. **상시로 알리지 않는다.** 상단 잠금 배너 · 채점기준 섹션 재연결 안내를 껐다
3. **저장할 때 한 번만, 모아서 묻는다.** 지워질 게 실제로 있을 때만

```
저장하면 지워지는 것이 있어요
· 채점 결과 28명분 — 채점기준이 바뀌어 점수의 근거가 달라져요.
· 과제물 인식 결과 28명분 — 활동지가 바뀌어 답안을 다시 나눠야 해요.
되돌릴 수 없어요. 취소하면 고치신 내용은 그대로 남아 있어요.
                                      [취소]  [지우고 저장]
```

**사전 예고가 없어도 되는 이유** — `취소`하면 고친 내용이 그대로 남고 저장만 안 된다. 잃는 게 없다.

### 남긴 것 — 줄리 우려 (올립이 못 박음)

> "줄리가 우려했던, 채점기준이 달라지면 아래 정확도 개선사항도 달라지는데
> 이거 **처리 표시는 놔둬야** 해. 그것도 날리면 안 돼."

**채점기준 변경 → ② 연결이 낡는다**는 표시는 **스위치와 무관하게 늘 켜 둔다.**
저장 확인이 이걸 못 잡기 때문이다 — 연결은 *지워지는 게 아니라* 낡을 뿐이라 다이얼로그에 안 뜬다.
표시가 없으면 교사가 영영 모른다. 남아 있는 자리: **고도화 카드의 `(다시 확인 필요)`** ·
**단계 칩 주황 점** · **②단 상단 안내**. `S.staleLink` / `S.staleAnswer` 그대로.

### 되살릴 수 있게 — 스위치 두 개 (지우지 않았다)

> "삭제했지만 내가 살리라고 할 수도 있는데, 목업 보고 너무 안내 적다고 하면 살릴 수도 있어."

파일 상단 `var S = {` 바로 위에 있다. **한 줄 `true`로 되돌아온다.**

| 스위치 | 기본 | `true`로 하면 |
|---|---|---|
| `SHOW_STANDING_NOTICE` | `false` | 상단 잠금 배너 + 채점기준 섹션 재연결 안내가 다시 뜬다 |
| `LOCK_AFTER_OCR` | `false` | 인식 후 활동지 교체·삭제·문항 재분할이 다시 막힌다 |

### 어디를 건드렸나

- `S.questionsDirty` 신설 — 인식이 끝난 뒤 활동지·문항을 건드리면 켜진다
  (`markQuestionsChanged` · `resetAdvWork`). 저장 확인의 두 번째 줄이 이걸 본다
- `saveDesign()` — 단일 조건에서 **지워질 것 배열**로. 없으면 안 묻고 바로 저장
- `pickWorksheet()` — 인식 후에도 열리고, 확인창에 `과제물 인식 결과도 … 저장할 때 지울지 물어봐요` 한 줄 추가

**검증**: 채점기준만 / 둘 다 / 깨끗한 설계 3분기 · 인식 후 교체 가능 · 줄리 표시 잔존 확인 ·
15케이스 615개 핸들러 오류 0건

## (참고) 결정 전 논의 — 미결 1

> "채점기준 달라지면 다 달라지는 거고 / OCR 인식한 뒤에 활동지 과제물 바꾸면 기존 꺼 리셋된다고
> 처리하는 게 나은데 그치 / 너 지금 상단 배너 나오는 거 별로거든. 미리 안내해주는 것보다
> 수정하고 저장할 때 묻는 게 나을 거 같은데.. 앞에 안내가 낫다고 생각하기도 하고"

**지금 목업의 상태** — 세 가지가 따로 논다.
`#lock-banner`(상시, 설계 상세 맨 위) = "인식이 시작되어 문항을 고칠 수 없어요" · "채점이 실행된 평가예요" /
`#rubric-relink`(상시, 채점기준 섹션 위) = "채점기준이 바뀌었어요, 문항 연결도 다시 봐 주세요" /
`S.staleLink`·`S.staleAnswer` = 단계 칩 주황 점. 게다가 **활동지 교체는 아예 막혀 있다**
(`pickWorksheet`: "복사해서 만들기로 새 설계를 만들어 주세요").

### 제안 (내 권고 — 결정 필요)

원리 하나로 정리한다. **막지 않는다. 되돌릴 수 없는 것이 사라지는 시점에 한 번 묻는다.**

| | 사라지나 | 왜 |
|---|---|---|
| 채점 결과(점수) | **사라짐** | 채점기준이 바뀌면 점수의 근거가 달라진다 |
| 과제물 인식 결과 | **사라짐** | 활동지·문항이 바뀌면 답안을 어느 문항 것으로 나눌지가 어긋난다 |
| 문항 연결 · 모범답안 | 안 사라짐 | "무엇을 보고 채점할지"라 다음 채점부터 적용되면 된다 |

1. **활동지 교체 잠금을 푼다.** 막으면 교사가 할 수 있는 게 없고 `복사해서 만들기`는 부담이 크다.
   대신 바꾸면 **인식 결과가 리셋된다고 알린다**(올립 제안 그대로)
2. **상시 배너는 없앤다.** 막지 않기로 하면 "고칠 수 없어요"라는 배너는 존재 이유가 사라진다.
   `#rubric-relink`의 "다시 봐 주세요"도 필수가 아닌 일을 상시로 재촉하는 꼴
3. **저장할 때 한 번, 지워지는 것을 모아서 묻는다**
   ```
   저장하면 지워지는 것이 있어요
   · 채점 결과 28명분 — 채점기준이 바뀌어 점수의 근거가 달라져요
   · 과제물 인식 결과 28명분 — 활동지가 바뀌어 답안을 다시 나눠야 해요
   [취소]  [지우고 저장]
   ```
4. **다만 "앞에 안내"를 완전히 버리진 않는다** — 열심히 고친 뒤 저장 순간에 처음 알면
   되돌리려고 고친 걸 다 버려야 한다. 그래서 **그 필드를 실제로 건드린 순간 그 섹션 안에 회색 한 줄**
   (`이대로 저장하면 채점 결과 28명분이 지워져요`). 상시가 아니라 **행동의 결과**라 성격이 다르고,
   저장 다이얼로그의 예고편이지 중복이 아니다
5. **조건을 좁힌다 — 지워질 게 실제로 있을 때만.** 채점 전 설계에선 아무것도 안 뜬다.
   대부분의 설계가 채점 전이므로 평소엔 조용하다

**올립이 망설인 지점**("앞에 안내가 낫다고 생각하기도 하고")은 4·5번으로 답한 셈이다 —
상시 안내는 버리되, 행동 시점 한 줄은 조건부로 남긴다. 이걸로 갈지 아니면 저장 확인 하나만 둘지 결정 필요.

### 손대기 전 알아 둘 것

- **판정은 하드코딩이 아니다.** `rvScan()`이 입력 텍스트를 훑는다 → 교사가 고쳐 쓰면 결과가 바뀐다.
  씨앗 5개에 돌리면 2건(채점기준 2·3). 시안2를 적용하면 **재검토 0건**이 된다
- **한글 문구를 `\uXXXX` 이스케이프로 쓰지 말 것.** 이번 세션에서 `시안2`→`시의2`, `바뀐`→`바뀜`,
  `뺄까요`→`뻐다요` 로 네 번 깨졌다. Write/Edit 도구로 **리터럴 한글**을 쓴다
- **CSS 블록을 잘라낼 땐 여러 줄 규칙의 뒷줄이 남는다.** 한 번 화면 절반이 안 그려졌다.
  자른 뒤 경계 앞뒤를 눈으로 확인할 것
- 검증은 매번 `applyCase` 전 케이스 × 보이는 `[onclick]` 전수 실행 + 중복 id. 14케이스 615개

## 세 단계의 "왜 하는지" — 공식 사유로 교체 (26-09-02 제공)

내가 쓰던 ①단 이득(`1-1, 1-2처럼 잘게 나뉜 문항도 경계가 확실해져요`)은 **폐기**.
진짜 이유는 **학생이 활동지에 해 놓은 낙서·메모를 AI가 문제 지시문으로 오독하는 걸 막는 것**이다.

| 단계 | 왜 하는지 (근거 원문) |
|---|---|
| ① 빈 활동지·문항 확정 | 문항 지시문을 미리 확정해 두면, 학생 제출물의 낙서·메모로 훼손된 지시문을 AI가 오독하는 것을 막는다 |
| ② 채점기준 연결 | 각 문항에 요구되는 채점기준만 적용해 평가한다 |
| ③ 모범답안·채점 가이드 | 선생님이 생각하는 "상" 수준을 AI가 파악해, 추론이 불가능한 답안도 채점할 수 있다 / 채점 가이드로 선생님의 채점 고려 사항을 반영한다 |

①단은 두 안 중 **1안(낙서·메모 오독)** 을 골랐다 — 2안("답안에 더 집중")은 무엇이 좋아지는지가
흐려 행동 유도가 안 된다. 2안으로 바꾸려면 `ADV_WHY[1]`·`ADV_GAIN[1]`·`ADV_LOAD.ws.why` 세 줄.

**같은 이득을 세 시점에 반복**한다 — 기다릴 때(`ADV_LOAD.why`) · 하는 동안(`ADV_WHY`, 탭 아래 상시) ·
마쳤을 때(`ADV_GAIN`, 1회). 과업이 무거워 한 군데서만 말하면 놓친다.
바깥 문구 두 곳(저장 직후 분기 팝업 · 설계 상세 고도화 카드)도 같은 말로 맞췄다.
업계어는 풀었다 — `훼손된 지시문 오독` → `낙서나 메모를 문제로 잘못 읽음`,
`"상" 기준 수준` → `잘한 답안의 수준`, `추론 불가능한 답안` → `미루어 짐작하기 어려운 답안`.

## ▶ 이어받기

AI팀이 **루브릭 검토 API를 실제로 만들었다**(맥 쪽 목업: `루브릭 AI 검토 목업` 아티팩트).
v8 설계 상세의 `채점기준` 섹션에 그 기능을 붙였다.

```
채점기준                      채점기준 5개 · 만점 25점
어떻게 입력해야 하나요?  채점기준 입력 예시   ← 쓰기 전에 필요한 도움말
채점기준 카드들 (짚힌 카드는 머리 아래 코멘트)
[✨ 채점기준 AI 생성] [+ 직접 추가]           ← 만드는 일
─────────────────────────────────────       ← 단을 가름
[ AI 검토 밴드 ]                              ← 다 쓴 뒤에 하는 일
```

**단을 가른 이유** — `AI 생성`·`직접 추가`는 *추가하는* 행위, `AI 검토`는 *다 쓴 뒤 점검하는*
행위다. 셋을 한 줄에 두면 검토가 "세 번째 추가 방법"으로 읽힌다.

**HowToUse를 카드 위로 올린 이유(09-02 올립 지적)** — 개발 화면은 이 줄이 섹션 **맨 아래**였다.
거기에 검토 밴드가 들어오면서 바닥이 `추가 / 도움말 / 검토` 세 덩이가 되어 짝이 안 보였고,
쓰기 전에 필요한 도움말인데 카드 5개를 지나야 보였다. 카드 앞으로 올려 바닥을 두 덩이로 만들었다.
⚠️ **개발 확정 화면과 다른 유일한 지점** — 개발 전달 시 짚을 것.

## 무엇을 짚는가 (API 계약)

AI팀 목업의 `issue_type` 두 가지를 그대로 쓴다. 화면 문구는 업계어를 걷어냈다.

| API | 화면 문구 | 제안 필드 |
|---|---|---|
| `overloaded_element` | 한 기준에 여러 가지가 섞여 있어요 | `suggestion.split_into[]` → 칩 |
| `ambiguous_boundary` | 배점 사이 경계가 흐릿해요 | `suggestion.revised_descriptions[]` → 전/후 |

응답 예(AI팀 목업 `기대 출력 JSON`):
```json
{"comments":[{"element_id":910002,"element":"표현","issue_type":"overloaded_element",
  "target_level_ids":[],"comment":"…","suggestion":{"split_into":["어법의 정확성","어휘 사용","분량"]}}]}
```

## 목업의 판정은 **진짜로 돈다** (하드코딩 아님)

올립 요구 = "실제로 사용자가 해보게 하고 싶다". 그래서 씨앗 데이터에 결과를 박지 않고
현재 입력된 채점기준 텍스트를 훑는 규칙을 넣었다 — 교사가 직접 고쳐 쓰면 판정이 따라 바뀐다.

- **섞임**: `RV_AXES` 8개 평가 축(어법/어휘/문장/분량/구성/근거/주장/기한) 중 **셋 이상**이
  한 기준에 잡히면 → 그 축들을 `split_into`로 제안
- **경계**: 세부 기준 문장에 정도 부사(`다소·대체로·매우·약간·조금·어느 정도·상당히`)가 있으면
- 한 기준에 **코멘트 하나**, 섞임이 우선(쪼개는 게 먼저다)

**고쳐 쓴 문장은 지어내지 않는다.** `RV_FIX`에 미리 적어 둔 줄만 전/후를 보여 주고,
교사가 직접 쓴 줄은 짚기만 한다 — [시스템이 아는 것만 표시] 원칙.

씨앗(`RUBRIC_SEED`, 논설문 5개)에 그대로 돌리면 **2건**이 나온다.
가짜 채점기준을 새로 끼우지 않고 원래 있던 것에서 걸린 것이다.

| | 걸린 이유 |
|---|---|
| 채점기준 2 (논리적 구성) | `연결이 **다소** 어색함` → 경계 흐릿 |
| 채점기준 3 (어휘·문장 표현) | 어법 + 어휘 + 문장 표현 3축 → 섞임 |

## 드로워 = 채점기준 전체를 고치는 작업대 (26-09-02 올립 지시, 2번 되물림)

**1차: 카드 안 인라인 코멘트 → 되물림**
> "저 수정한 안내 사항 거슬리는데 고쳐도 계속 남아있으니까 별로야"

카드 안에 상자를 두면 **고치는 내내 자리를 차지하고 배점 표를 아래로 민다.**

**2차: 읽기 전용 드로워 → 되물림**
> "나는 채점기준 전체 표시해주고 고칠것은 드로워내에서 고친다음 덮어쓰게 하는 게 나을거같은데"

읽는 곳과 고치는 곳이 갈라져 있으면 **닫고 → 카드 찾고 → 다시 타이핑**이다.

**3차(현행): 우측 드로워에 채점기준 전체를 펴고, 거기서 고쳐 덮어쓴다.**
v2·v3의 `채점기준 AI 생성` 드로워와 **같은 규격**
(overlay `rgba(0,0,0,.35)` · 우측 800px · 헤더 18px/700 + X · 스크롤 바디 24px · 푸터 border-top).

- **짚힌 것만이 아니라 5개 전부** 편집 가능 — 옆 기준을 보며 고쳐야 결이 맞는다
- 짚힌 기준은 `1.5px 주황 테두리` + 그 안 맨 위에 AI 코멘트(하늘색). 배지는 안 단다(코멘트 제목과 같은 말)
- **제안 문장은 그 배점 줄 바로 아래**에 `✨ 이렇게 … [적용]` — 멀리 두면 어느 줄 얘기인지 모른다
- 800px이라 제목을 표 옆이 아니라 **위에 쌓았다** — 고치는 건 세부 기준 문장이라 폭을 다 준다
- **복사본(`DW_R`)을 고친다.** `채점기준에 반영`을 눌러야 진짜가 바뀌고,
  안 누르고 닫으면 `고친 내용 버리기` 확인. 카드에서 고칠 때와 같은 연쇄가 걸린다
  (문항 연결 stale · 채점 후면 저장 시 초기화 확인 · 검토 결과 무효화)
- **`적용`은 AI가 고쳐 주는 게 아니다** — API가 이미 돌려준 `revised_descriptions`를
  선생님이 필드에 넣는 것. 넣고 나서 더 고쳐도 된다.
  누르면 **제안 줄은 사라진다** — 필드가 바뀐 걸 눈으로 보는데 "적용됨"을 또 말하면 자리만 차지

## 시안2 — 별도 페이지 좌우 비교 (26-09-02 올립 제안) · **시안1과 나란히 보존**

> "아예 페이지로 빼서 왼쪽은 사용자 입력한 거, 오른쪽은 AI가 의견쓰고 바꿔준거 …
> 사용자가 일일이 수정해줄 필요없이 AI가 의견주고 바꾼거고 좌우 있으니까 뭐가 달라졌는지 한눈에"

| | 시안1 `#case=rv-hit` | 시안2 `#case=rv-page` |
|---|---|---|
| 모습 | 우측 드로워 800px | **별도 화면** `screen-review` (설계 상세와 같은 크롬) |
| 오른쪽에 오는 것 | AI 코멘트 + 제안 문장 1줄 | **AI가 다듬은 채점기준 통째로** |
| 교사가 하는 일 | 필드를 하나씩 고침 | **골라서 반영**(체크) |
| 지금 API로 되나 | ○ (`revised_descriptions` 확인 필요) | **✕ 맥에게 요청할 스펙** |

**둘 다 남긴다** — 까망이에서 번갈아 보며 고른다. `S.rvPlan`(1\|2)이 밴드 버튼과
`runRubricReview()`의 목적지를 가른다.

### 오른쪽 셀 = 위 AI 의견 / 아래 고칠 수 있는 채점기준

올립이 물었다 — "AI 의견을 각각 만들지 말고 상단에 합치는 것도 가능할 것 같은데, 어느 쪽?"
→ **행별로 둔다.** 오른쪽이 편집 가능해졌으므로 **고치는 칸 바로 앞에 근거가 있어야** 한다.
위로 몰면 아래에서 고치다가 "왜 이렇게 바꿨더라" 하고 되올라가야 하는데,
그게 드로워에서 이미 겪은 스크롤 왕복이다. [[feedback_ux_reading_flow]]

**둘 다 쓰되 역할을 나눈다** — 맨 위 한 줄은 *몇 곳·무슨 종류*(조망),
행별 오른쪽 위는 *이 기준을 왜 이렇게 바꿨는지*(근거). 같은 말을 두 번 하지 않는다.

- 오른쪽 제목·배점·세부기준 전부 입력칸. `AI 안으로 되돌리기`로 처음 제안 복구
- **입력칸이 글자처럼 보이면 고쳐도 된다는 생각을 못 한다**(26-09-02 올립: "클릭 이벤트 못 느꼈어").
  `border:none`을 걷고 **왼쪽=회색 바탕 평문 / 오른쪽=흰 바탕 입력칸**으로 갈랐다.
  hover는 테두리 `#89C0F3`+바탕 `#F7FBFF`, focus는 `#0950A0`+3px 링 `#D5EBFF`.
  첫 블록 위에 `✎ 여기서 바로 고쳐 쓸 수 있어요`(맨 위 리드에서 같은 말은 뺐다)
  ⚠️ **`:focus`는 변경 줄(`tr.chg input`) 규칙보다 뒤에 둘 것** — 앞에 두면 같은 순위라 덮인다
- 고친 값은 `RV_EDIT`에 남아 **체크를 껐다 켜도 안 사라진다**
- 입력 중에는 다시 그리지 않고(커서가 튄다) **만점만 그 자리에서 갱신**
- **만점은 어긋났을 때만 뜬다** — 상시 `만점 25점 / 25점`은 뺐다(AI 의견 라벨에 `만점 5점은 그대로`가
  이미 있어 중복이고, 늘 떠 있으면 정작 깨졌을 때 눈에 안 들어온다). 어긋나면 주황으로
  `만점이 25점 → 27점으로 달라졌어요`. 만점이 변하면 이미 매긴 점수가 근거를 잃는다
- 반영 버튼은 **`수정된 채점기준 적용 (2)`** — `고른 것 반영`은 무엇을 어디에 넣는지가 없었다.
  0개면 개수 없이 라벨만 + 비활성(비활성이 "못 한다"를 이미 말하므로 라벨을 바꾸지 않는다)

### 시안2가 성립하려면 AI가 더 줘야 하는 것
`split_into`(축 이름)만으로는 오른쪽을 못 채운다. **제목 + 배점 배분 + 세부기준 전부**가 필요하다.

- **배점은 만점을 지키게 나눈다** — 채점기준 3(만점 5점) → 2+2+1점.
  좌우 머리에 `만점 25점` / `만점 25점`이 나란히 떠서 **안 깨진다는 게 한눈에** 보인다.
  만점이 변하면 채점 결과가 무효가 되므로 이게 이 제안의 핵심 제약이다
- 배점은 제안일 뿐이라 `배점은 만점을 지키게 나눈 제안이에요. 반영한 뒤 설계 화면에서 고쳐도 돼요` 주황 안내
- 제목은 축 이름(명사형)이 아니라 **`~을 평가한다.` 서술형**으로 — 나머지 채점기준과 평행하게

### 검증에서 나온 증거
씨앗에 시안2를 통째로 반영하면 **재검토 결과가 2건 → 0건**. 기준 5개 → 7개, **만점 25점 그대로**.
제안이 실제로 지적을 해소한다는 뜻이다.

## 두 지적은 성질이 다르다 — 고치기 vs 늘리기 (26-09-02 올립)

> "기존 있는 걸 삭제하는 건 OK, 없는 걸 새로 추가하게 하는 거가 고민이거든"

| | 경계 흐릿 | 섞임 |
|---|---|---|
| 고치는 방식 | 기존 문장 **교체** | 채점기준을 **늘림** |
| API가 주는 것 | `revised_descriptions` — 완성된 문장 | `split_into` — **축 이름뿐** |
| 만점 | 안 변함 | **변함** → 채점 결과 무효 |
| AI가 지어내나 | 아니오 | **예**(문장 9줄·배점 배분) |

섞임을 자동화하려면 AI가 축마다 세부 기준을 새로 쓰고 20점을 7/7/6으로 쪼개야 한다.
**API에 없고, 배점 배분은 교사의 평가 의도**다.

→ **"없는 걸 만들기"를 "있는 걸 복사하기"로 바꿔 푼다.**
드로워 채점기준 머리에 **`복제`·`삭제`를 카드와 똑같이** 둔다. 섞임 코멘트는 안내 문장만
(같은 상자 두 줄 위에 버튼이 있어 중복). 원본의 사본이 바로 아래 생기고, 교사가 각각 하나씩만 남긴다.
**시스템이 지어내는 게 0**이고, 만점이 늘어난 건 토스트와 총점 표시로 바로 보인다.
사본에는 코멘트가 안 붙는다 — AI가 판정한 건 원본이니까.

**삭제도 복사본에만 든다.** 카드의 삭제는 "되돌릴 수 없습니다"지만 드로워는
`채점기준에 반영`을 눌러야 진짜로 지워지므로 그렇게 말하지 않는다 — 버리고 닫으면 그대로다.
코멘트 본문에서 **채점기준 번호를 뺐다**(`이 기준이 …`) — 복제·삭제로 순번이 밀리면 틀리기 때문.

기각한 길 둘 — **빈 채점기준 N개 생성**은 배점이 비어 설계가 미완성으로 잠기고 빈칸 9개를 던진다.
**AI가 다 써 주기**는 API에 없는 걸 목업에서 지어내는 것이라 [[feedback_system_honest_signaling]] 위반.

## 밴드는 **한 모습만** 쓴다 (26-09-02 올립, 두 번에 걸쳐 줄임)

> "여기 배너는 상태에 따라 계속 달라지는 게 아니라 하나로 처리하면 어때?
> 쓰려면 쓰고 말려면 마는 기능인데 넘 복잡도 높이는 거 같아서"

처음엔 4가지 얼굴이었다 — 파랑 권유 / 주황 `n곳 짚음`+점프 칩 / 초록 통과 / 파랑 stale.
**선택 기능인데 읽는 비용이 기능값보다 커졌다.** 하나로 고정했다.

```
✨ AI가 헷갈릴 만한 곳을 미리 짚어 드려요
   한 기준에 여러 가지가 섞여 있거나 배점 사이 경계가 흐릿하면 알려 드려요.
   그런 곳을 먼저 고쳐 두면 AI가 매기는 점수가 선생님 기준에 더 가까워져요.
   [ 채점기준 AI 검토 ]
```

- 검토 전·후·통과·고친 뒤 **전부 같은 문구**. 조건부는 잠금 사유 한 줄뿐
  (`채점기준을 다 입력하면 검토할 수 있어요` + 버튼 비활성), 채점기준 0개면 숨김
- **결과가 아직 살아 있으면 다시 돌리지 않고 바로 연다** — 버튼이 하나라도 5초를 또
  기다릴 이유는 없다. 채점기준을 고치면 `rvInvalidate`가 치워 주므로 늘 최신
- 결과가 어땠는지는 **드로워/검토 화면이** 말하고, 짚힌 곳은 **카드 주황 테두리**로 남는다.
  밴드는 '들어가는 문'일 뿐
- 함께 걷어낸 것 — 카드로 점프하는 칩(고치는 곳이 카드가 아니라 드로워라 갈 일이 없다),
  `rvJump`·깜빡임 CSS, `.rv-box.rv-ok`/`.rv-hit` 색 규칙, `S.reviewRan`

로딩 중에는 밴드를 비운다(스피너는 드로워/검토 화면이 돌린다).

검토를 누르면 **드로워가 먼저 열리고 그 안에서 돈다** — 결과가 나올 자리에서 기다리게.

- **채점기준 미입력이면 버튼 비활성** + 잠금 사유를 버튼 **앞**에
- **막지 않는다** — "그대로 둘 수도 있어요, 채점은 막지 않아요"
- **고치면 결과가 사라진다** — 검토는 그때 그 문장을 본 결과라, 채점기준을 건드리면
  코멘트를 걷고 `채점기준이 바뀌었어요 / 다시 검토해 보세요`로 되돌아간다.
  입력 중에는 카드를 다시 그리지 않고(커서가 튄다) 붙어 있던 것만 DOM에서 걷어낸다

## 까망이 케이스 +3 (총 15)

`rv-hit`(시안1 드로워) · `rv-page`(시안2 좌우 비교) · `rv-ok`(고쳐 놓은 뒤 0건).
`rv-ok`는 씨앗의 걸린 두 기준만 고쳐 쓴 `RUBRIC_CLEAN`을 쓴다 — 없는 채점기준을 새로 짓지 않음.

## 올립 확인 필요

1. **맥에게 확인 — `revised_descriptions`를 실제로 내려주는가.**
   AI팀 목업의 `기대 출력 JSON`에는 있으나 진짜 응답에 있는지 미확인.
   `적용` 버튼이 이것 하나에 달려 있다(올립: "이건 좋은 기능이지만 AI에서 저렇게 못 할 것 같은데
   … 근데 필요한 거 같다" → 남기고 요청하기로). 없다면 `dwRubricHtml`의 `dw-sug` 행만 지우면 된다
2. PRD — H-2608 V1.0에 이 기능이 없다. 추가 필요
3. 밴드 위치 — 지금은 채점기준 섹션 맨 아래(누른 자리에서 결과가 보이게)

## 검증

15케이스 680개 핸들러 오류 0건 · 중복 id 0 · 상태 4종 문구 전수 ·
시안2 좌우 정렬 5행 전부 top·height 일치(539px씩, 가로 넘침 없음) ·
시안2 편집(제목·배점·세부기준 → `RV_EDIT` 반영 · 체크 토글해도 보존 · 되돌리기로 AI 원안 복구) ·
만점 어긋남 표시(2→4점 고치니 `만점 27점 · +2점 달라짐` 주황, 되돌리면 해제) ·
시안2 반영 후 재검토 2건 → **0건** · 만점 25→25 유지 · 선택 해제 시 버튼 비활성 ·
복제(기준 5→6 · 만점 25→30 · 사본엔 코멘트 안 붙음) · 삭제(확인 → 복사본만 빠짐 → 반영해야 진짜 삭제) ·
드로워 편집 흐름(적용→복사본만 변경→손편집→반영→원본 덮어씀→검토 무효화→밴드 stale) ·
버리고 닫기 확인 · **고친 뒤 재검토하면 2건 → 1건으로 줄어드는 것까지 확인**(판정이 진짜로 돈다는 증거)

⚠️ **CSS 블록을 잘라낼 때 여러 줄 규칙의 뒷줄이 남았다** — `.rv-fix-b`가 2줄이라
첫 줄까지만 자르고 `background:#fff; … }` 한 줄이 고아로 남아 이후 규칙이 통째로 죽었다.
잘라낸 뒤에는 반드시 경계 앞뒤를 눈으로 확인할 것. [[feedback_bulk_edit_safety]]
