# 2026-09-01 — 평가 설계 v8: 진단 걷어내고 "AI 채점 정확도 높이기" 3단계

작업본 `../output/evaluation_design_v8.html` (v7은 보존, 손대지 않음)

## ▶ 이어받기

**설계는 채점기준까지 입력하면 완료.** 그 위에 얹는 선택 흐름이 `AI 채점 정확도 높이기` 세 단계다.

```
설계 상세(기본정보·성취기준·채점기준·점수계산·결시자) → 저장 = 설계 완료
   └ [빈 활동지가 있나요?]  ← 저장 직후 1회, 이미 올린 설계엔 안 물음
        ├ 있어요 → ① 빈 활동지·문항 확정 → ② 채점기준 연결 → ③ 모범답안 → 마치기
        └ 없어요 → 목록
   재진입: 목록 카드의 「AI 채점 정확도 높이기 n/3」 버튼(지름길) 또는 설계 상세의 카드
```

- **되돌아가기 O / 건너뛰기 X.** 뒤 단계가 앞 단계 산출물을 참조한다(문항이 나뉘어야 연결·답안이 성립). 못 가는 단계도 눌리게 두고 왜 못 가는지 토스트로 말한다
- **중간 이탈 = 하던 데까지 남음.** ①만 저장하면 ②③은 빈 채로
- **저장은 머문다.** ①②의 `저장`은 그 자리에 남고, 나가는 건 `이전 단계`·`설계로 돌아가기`와 ③단 `저장하고 마치기`만

## 걷어낸 것 (v7 → v8)

AI 진단·적합도·총평·판정 태그, AI가 다시 짠 채점기준 제안, 재매핑 제안 — **전부 삭제**.
어휘 단위로 훑어 `진단`·`적합도`·`총평`·`보완 필요` 잔재 0건, 죽은 CSS 블록 5개(rt-ro·cmp-*·apply-band·diag-card·link-tb)와 마스터-디테일 잔재(.s3-q-item·_s3Cur·renderS3List/Detail·selectQ3·s3SelectAll)도 제거. 147KB → 117KB.

## 확정 사항

| 항목 | 결정 |
|---|---|
| 기능명 | `AI 채점 정확도 높이기` (동사형 유지 — 필수 설계 섹션과 축이 달라 평행성 대상 아님) |
| `(선택)` 라벨 | **뺌** — "안 해도 되는 것"으로 읽혀 넘어감 |
| 카드 강조 | 옅은 파랑 배경 `#F7FBFF` + 테두리 `#B5D6F9` + sparkles 아이콘. 좌측 띠 안 씀 |
| 잠금 | 채점기준 미입력이면 카드 버튼 비활성 + 잠금 사유를 버튼 **앞**에. 딥링크로도 못 들어옴 |
| 목록 카드 | `설계 완료` 배지 제거(기본값이라 알릴 게 없음), `임시 저장`만 유지. 날짜는 타이틀 아래 |
| 목록 지름길 | 하단 전폭 버튼 `AI 채점 정확도 높이기 n/3`. **카드 본체는 설계 상세로, 이 버튼만 고도화로** |
| 3/3 카드 | 잠그지 않음 — 다 채워도 고치러 갈 수 있어야. 색만 낮춤(회색 톤) |
| 문항 용어 | **AI가 "나눈다" / 교사가 "확정한다"** — 주체로 갈라 씀 |
| 로딩 | ① 문항 분석 · ② 채점기준 연결 둘 다 대기(실제 30초 내외). **화면 벗어나면 처음부터 다시** — 문구뿐 아니라 동작도 그렇게(타이머 취소) |
| 연결 재생성 | `linkReady`로 한 번만. 되돌아와도 안 돌림. 문항이 새로 생길 때만 무효화 |

## ②단 = 문항 × 채점기준 매트릭스 (2026-09-01 결정)

맥이 **좌우 두 열 + 선 긋기**를 제안했으나 채택하지 않음.
- **선 매칭은 N:M에서 무너진다** — 문항 10 × 기준 10이면 선이 최대 100개, 교차하면 못 읽음
- 양쪽 열 독립 스크롤 시 좌표 어긋남 · 드래그 정확도 · 키보드 조작 불가

**다만 맥이 짚은 문제(전체 조망 안 됨)는 실재**했다. 기존 마스터-디테일은 문항마다 클릭해야 하고, 무엇보다 **다 봤는지 스스로도 모른다.** AI가 이미 연결해 두므로 이 화면의 주 과업은 **확인**이고, 확인 과업에 클릭이 드는 건 손해.

→ **매트릭스 채택.** 행=문항, 열=채점기준, 칸=체크.

- 머리 두 단: 위 `채점기준`(열 묶음) / 아래 번호. **번호는 범례와 같은 파란 원 배지** — 숫자만 두면 위를 뒤져 짝지어야 함
- 상태는 **별도 열**(`연결` 96px) — 문항 길이에 밀리지 않게. 칩 3종: 🔴 `미연결` / 🟠 `확인 필요`(채점기준 변경 후) / 🟢 `연결완료`. 기존 `.badge` 재사용
- 지시문은 **한 줄 말줄임** — 두 줄이면 행 높이가 제각각이라 훑기 어려움. 전문은 hover
- `table-layout:fixed` + **colgroup 필수** — 첫 행이 colspan이라 colgroup 없으면 열이 균등 분할돼 문항 열이 100px로 쪼그라듦
- AI 이유는 표 **아래로** 모음(칸마다 붙이면 표가 안 읽힘). 배경 하늘색 `#F3F9FF` + 테두리 `#D9EAFB`

**실측(최대 조건 기준 10 × 문항 10)**: 문항열 502px · 상태열 96px · 체크셀 48px · 가로 넘침 없음 · 행 높이 균일.

## 까망이 케이스 12종

`list` / `new` / `ready` / `ask`(저장 직후 분기 질문) / `adv-upload` / `adv-1` / `adv-2` / `adv-3` / `adv-linked`(②까지 하고 나감) / `adv-done` / `locked` / `scored`
옛 해시(`#case=sheet-answer`·`result-ok`·`edited` 등)는 새 케이스로 넘겨 공유 URL이 죽지 않음.

## 열린 것

- **확인 다이얼로그 제목 형태 혼재** — 명사형(`채점기준 삭제`) vs 의문형(`활동지를 지울까요?`). 성취기준·채점기준 삭제 모달이 개발 확정본인지 확인 후 통일
- **채점 후 잠금 정책** — v7에서 미결로 넘어옴. 현재는 "배점 변경·채점기준 삭제만 잠금". 올립 제안인 **"채점기준이나 문항 연결을 바꾸면 채점 결과가 지워진다" 단일 규칙**은 미확정
- PRD 재작성 (현 V0.9는 v7 기준)
- `output/index.html` 목업 목록 카드 — **올립이 "픽스했다"고 할 때** 갱신(2026-09-01 지시)

## 빌드 방식 (재현용)

목업은 손으로 고치지 않고 **v7에서 스크립트로 생성**한다. 스크립트는 세션 스크래치패드에 있고, 사라졌으면 v8을 직접 편집해도 된다.
검증은 매 빌드마다 ① 케이스별 보이는 `[onclick]` 전수 실행(479개) ② 실제 `File` 주입 업로드 ③ 중복 id ④ 폐기 어휘 grep.
